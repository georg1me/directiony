#x is the actual direction while y is the ideal direction
import math

def directiony(x,y):
            optimal = y
            dir_radians = (math.cos((x - optimal)))
            return(dir_radians)
